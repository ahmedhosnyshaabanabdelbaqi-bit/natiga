# W1-D · Administration backbone: settings, modules, users, roles, audit viewer, security events, banners, failed jobs, operations exception center & incidents, install/health/setup, sessions, 503   (test DB: ev_test_4)

Owned paths: `app/Modules/System/**` (may only ADD keys to `Settings.php`; keep `Services/{Settings,SettingsRegistry,Modules,DashboardKpis}.php` public APIs), `app/Modules/Rbac/**` (extend, don't break `PermissionRegistry`/`RbacSync`), `app/Modules/Audit/Http/**`, `app/Modules/Audit/routes/**`, `app/Modules/Audit/Permissions.php` (add keys), `app/Modules/Auth/routes/**`, `app/Modules/Auth/Http/Controllers/Settings*/**`, `app/Modules/Reports/Operations/**` + the "operations" section of `app/Modules/Reports/routes/admin.php` + keys operations.*/incidents.* in `app/Modules/Reports/Permissions.php`, `database/migrations/2026_01_01_000800_core_create_role_meta_table.php`, `2026_01_24_000200_reports_operations_create_tables.php`, `database/factories/{System,Rbac,Reports}/**`, `lang/{ar,en}/{system,users,roles,audit,operations}.php`, `resources/js/pages/admin/{settings,modules,users,roles,audit-logs,security-events,banners,jobs,operations,incidents,setup}/**`, `resources/js/pages/settings/sessions.tsx`, `resources/js/features/{system,users,operations}/**`, `resources/views/errors/**`, `tests/Feature/{System,Rbac,Audit,Operations}/**`, `docs/modules/system-admin.md`.

State: backend partially written (System ~46 PHP files, Reports ~21, both migrations). No pages and no tests yet. Audit and complete:
1. Settings `/admin/settings` (settings.view / settings.manage): tabs from `SettingsRegistry::grouped()`, typed inputs by definition type, validation from each definition's `rules`, save per group via `Settings::setMany` (audited), sensitive values masked, reset-to-default with confirmation, branding image upload (finfo MIME, ≤ 2 MB, public disk `branding/`), live preview.
2. Modules `/admin/modules` (modules.manage): toggles, core locked, reason required (stored via `Modules::setEnabled`), experimental badge.
3. Users `/admin/users` (users.view / users.manage; roles.manage for roles): list users holding staff/partner roles or `admin.access`/`partner.access` (search, filters role/status/MFA, sort); create (name, email, mobile, roles, direct permissions, locale) → password setup link (`Password::broker()->sendResetLink`), audited; edit; server-side rules (tested): only owner/super-admin assign/remove owner/super-admin; nobody edits their own roles/permissions; granting permissions the actor lacks is forbidden unless super role; audit `users.roles_changed` + SecurityEvents `role_changed` / `permission_escalation`; disable (reason; `SessionManager::logoutAll`; audit + `account_disabled`); reactivate; reset access (revoke sessions + reset link + optional MFA reset with reason → `mfa_reset_by_admin`); detail tabs profile / roles & permissions / login history / security events / active sessions (+ log out everywhere).
4. Roles `/admin/roles` (roles.manage): permission × role matrix grouped by module with AR/EN labels; save via `syncPermissions` + audit `roles.permissions_changed`; super roles read-only; custom roles (slug + AR/EN names in `role_meta`); delete only custom roles with no users; `RbacSync` seeds `role_meta` from `PermissionRegistry::ROLES`.
5. Audit log `/admin/audit-logs` (audit.view): filters actor, action (distinct list), entity type/id, request id, date range; detail drawer with old/new diff (redacted shown as `[redacted]`), reason, IP, UA; streamed CSV export (max 10k, audited `audit.exported`).
6. Security events `/admin/security-events` (security_events.view) with filters + "critical" quick filter.
7. Status banners CRUD `/admin/banners` (banners.manage).
8. Failed jobs `/admin/jobs/failed` (jobs.manage): list (job display name, queue, failed_at, first 300 chars of exception only), retry (`queue:retry`), delete; queue/scheduler health card (pending jobs per queue; scheduler heartbeat cache key `ev.scheduler.heartbeat` written by an every-minute scheduled closure; > 5 min stale = warning).
9. Operations Exception Center `/admin/operations` + incidents `/admin/incidents`: `OperationsExceptions::raise(category, severity, title, details, dedupKey, source)` (dedup increments occurrences; P0 → security event + notify `operations.manage` holders via `App\Modules\Notifications\Services\Notify` if it exists), resolve/assign/ignore with reason + audit; `HealthChecks::register(key, fn)` + `ev:daily-checks` (daily 06:00 Africa/Cairo) with checks: failed jobs, stale scheduler heartbeat, disabled users with sessions, unavailable integrations (if Integrations exists), purge expired idempotency keys; incidents (INC numbers, timeline, post-incident review fields, audited transitions). Permissions operations.manage, incidents.view, incidents.manage.
10. `ev:install` (first owner; interactive or `EV_OWNER_NAME/EMAIL/PASSWORD`; refuses if an owner exists; strong password; RbacSync), `ev:health` (DB, Redis if configured, queue table, storage writable, scheduler heartbeat; non-zero exit on failure), `/admin/setup` checklist (settings.manage).
11. `resources/views/errors/503.blade.php` (bilingual, messages from `system.maintenance_message_ar/en`, branding colors, no technical details). Sessions page: `app/Modules/Auth/routes/shared.php` → GET `/settings/sessions`, POST `/settings/sessions/logout-others` (requires `current_password`), DELETE `/settings/sessions/{id}`; page `resources/js/pages/settings/sessions.tsx` (rendered in the settings layout); audit/security event on logout-others.

Tests: settings save needs permission & is audited; sensitive masked; module disable blocked for core & requires reason; support-agent cannot change roles (403); accountant cannot grant self super-admin (403 + security event); operations-manager cannot grant a permission they lack; owner can; disabling a user deletes their sessions; audit immutability (model guard + DB trigger); exception dedup; `ev:install` refuses a second owner; logout-others deletes only other sessions.
