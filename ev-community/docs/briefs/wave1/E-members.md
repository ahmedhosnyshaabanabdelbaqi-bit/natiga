# W1-E · Members, membership card & QR verification, referrals, privacy   (test DB: ev_test_5)

Owned paths: `app/Modules/Members/**` (keep the public API of `Membership`, `MembershipStatus`, `MembershipStatusHistory`, `Settings.php`, existing controllers), `app/Modules/Referrals/**`, `database/migrations/2026_01_02_*_members_*.php`, `2026_01_22_0005*_referrals_*.php`, `database/factories/Members/**`, `lang/{ar,en}/{members,referrals,privacy}.php` (keep existing keys), `resources/js/pages/admin/{members,referrals}/**`, `resources/js/pages/member/{membership-card,privacy,referrals,profile}/**`, `resources/js/pages/member/{dashboard,status}.tsx`, `resources/js/pages/partner/members/**`, `resources/js/pages/public/verify/**`, `resources/js/features/members/**`, `tests/Feature/{Members,Referrals}/**`, `docs/modules/members.md`.

State: backend largely written (~66 PHP files in Members, ~10 in Referrals, both migrations). No pages, no tests. Audit and complete:
1. Data: `membership_verifications`, `account_deletion_requests`, `member_notes`; `member_referrals` filled on registration (listener on `Illuminate\Auth\Events\Registered` when the membership has `referred_by`), status → approved when the membership is approved.
2. `Actions/ChangeMembershipStatus` transitions: pending→active|rejected, active→suspended|expired, suspended→active, rejected→pending, expired→active; history row + audit `members.status_changed` (reason required for suspend/reject); suspend logs out all sessions (`SessionManager::logoutAll`) + security event; events MembershipApproved/Suspended/…; notification via `App\Modules\Notifications\Services\Notify::send(...)` behind a `MemberNotifier` service (the Notifications module now exists — call it for real, with dedup keys).
3. `Services/MembershipQr`: `token(Membership)` = base64url("{public_id}.{expires}.{hmac}"), hmac = HMAC-SHA256(app key, "{verification_token}|{expires}"), TTL from `members.qr_token_ttl_minutes`; `verify(token)` constant-time → valid/expired/invalid/not_active; rotating `verification_token` invalidates old tokens; QR SVG via endroid/qr-code 6. Member page `/account/membership-card` (premium card: site name, member name, member number in `<Code>`, status badge, join date, auto-refreshing QR via `router.reload({only:['qr']})`, regenerate with confirmation, print styles). Verification: `POST /admin/members/verify` (members.verify) and `POST /partner/members/verify` (partner.access; minimal payload: valid, name, member number, status — nothing financial), both logging `membership_verifications` with a whitelisted purpose; scan pages `/admin/members/scan`, `/partner/members/scan` (QrScanner if available, else manual token input); PUBLIC `GET /verify/{token}` (shared routes, throttle:public-forms) showing only valid/expired/invalid + masked number (EV-****23), logged purpose=public.
4. Admin `/admin/members` (permissions members.view/edit/approve/suspend/export/verify/notes/delete_requests with defaults: operations-manager all; support-agent view/notes/verify; accountant view): DataTable (search number/name/email/mobile, filters status/governorate/joined range/referral source, sort whitelist, bulk approve with confirmation, header counts by status); show page tabs profile / membership (status Timeline, referral code, referred by, verifications) / notes (add, pin) / consents / security events / deletion requests; actions approve, reject (reason), suspend (reason), reactivate, expire, edit profile (audited old/new), resend verification email; CSV export (members.export, audited); `/admin/members/pending`.
5. Member portal: dashboard nudge when mobile/governorate missing; `/account/profile` (governorate, mobile, locale, referral source); `/account/referrals` (when `referrals.enabled`: code, share link `/register?ref=CODE` + copy, counters, referred members first name + status only); `/account/privacy` (plain-language data summary, marketing consents email/sms/whatsapp writing `consent_logs` accepted/withdrawn rows, "deactivate my account" requiring `current_password` → status disabled + logout all + audit `members.self_deactivated`, "request deletion of personal data" → `account_deletion_requests`); admin `/admin/members/deletion-requests` with `Actions/AnonymizeMember` (name 'Deleted Member', email `deleted-{id}@anonymized.local`, mobile null, disabled; memberships/audit/financial rows kept; audit `members.anonymized` with reason).
6. `docs/modules/members.md`.

Tests: IDOR (member → /admin/members/{x} 403; staff without members.view 403); approve needs members.approve + writes history + audit; suspend requires reason & deletes sessions; invalid transition → 422; QR valid/expired/tampered/rotated/suspended; partner verify payload has no email/financial fields; public verify masks number; export permission; deletion request → anonymize keeps membership + audit rows; referral row created on registration with ref code and approved on approval.
