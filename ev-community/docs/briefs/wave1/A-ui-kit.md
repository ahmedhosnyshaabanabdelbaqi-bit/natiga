# W1-A · UI kit, shared components, starter-page localisation   (test DB: ev_test_1)

Owned paths: `resources/js/components/ui/**`, `resources/js/components/shared/**` (incl. `README.md`, `index.ts`), `resources/js/components/{manage-two-factor,two-factor-setup-modal,two-factor-recovery-codes,manage-passkeys,passkey-item,passkey-register,passkey-verify,delete-user,appearance-tabs,breadcrumbs,user-info,heading,input-error,text-link,password-input,alert-error}.tsx`, `resources/js/hooks/**`, `resources/js/pages/auth/{forgot-password,reset-password,verify-email,two-factor-challenge,confirm-password}.tsx`, `resources/js/pages/settings/{profile,security,appearance}.tsx`, `resources/js/layouts/settings/layout.tsx`, `lang/{ar,en}/settings.php`, keys you ADD to `lang/{ar,en}/auth.php`, `app/Http/Controllers/Settings/**`, `app/Http/Requests/Settings/**`, `app/Actions/Fortify/ResetUserPassword.php`, `tests/Feature/Settings/**`.

Already present: the shadcn primitives (tabs, popover, switch, radio-group, scroll-area, accordion, alert-dialog, progress, slider, hover-card, textarea, table, command, kbd, pagination, date-input, empty, drawer, field) and shared components (confirm-dialog, copy-button, data-table, filters-bar, use-query-state, form-field, inline-alert, pagination, phone-number, progress-bar, rating, section-card, skeletons, step-indicator, timeline, tone, plus the original page-header, empty-state, error-state, status-badge, money, date-time, stat-card, kpi-grid, language-switcher, status-banners). Audit them for RTL correctness (logical properties `ms/me/ps/pe/start/end`, `rtl:rotate-180` on direction icons), accessibility (labels, roles, keyboard), strict types (no `any`), and that they compile.

Still to build (verify first):
- `file-upload.tsx` — `FileUpload` single/multiple, accept, maxSizeMb hint, image previews, remove, drag & drop, keyboard accessible, controlled `value`/`onChange`, `error` prop; no upload by itself (document the Inertia `useForm` + `forceFormData` pattern).
- `qr-scanner.tsx` — `QrScanner` wrapping `html5-qrcode` via dynamic import (not in the initial bundle): permission handling with AR/EN messages, camera select (rear by default, `facingMode: 'environment'` for iOS Safari), torch toggle when supported, manual code input fallback, `onScan(text)` debounced so the same code isn't emitted twice within 3 s, `paused` prop.
- `map.tsx` — `MapView` (react-leaflet, tiles from `boot().map.tile_url`, attribution, markers + popups, grid clustering when > 200 markers, debounced `onBoundsChange` (500 ms), fallback message `t('core.states.map_unavailable')` when tiles keep failing or provider is `none`, `UserLocationButton` with lazy geolocation and graceful denial) and `AddressMapPicker` (draggable marker, lat/lng inputs, optional `onGeocode(query)` prop). Import `leaflet/dist/leaflet.css` in the module and fix the default marker icon URLs (Vite).
- `command-palette.tsx` — `CommandPalette` (cmdk in a Dialog, Ctrl/Cmd+K, groups of items `{label, hint?, icon?, href? | onSelect?}`, async `search(query)` prop, keyboard navigable).
- `index.ts` barrel exporting every shared component, and `README.md` documenting each component's props with a usage example (this README is the contract for all other engineers).
- Localise every hardcoded English string in the starter pages/components listed above using `t()` with keys in `lang/{ar,en}/settings.php` (new) or added to `auth.php` (never remove/rename existing keys). Settings layout nav: add "Sessions" → `/settings/sessions` and "Privacy" → `/account/privacy` (the latter only when `auth.user.is_member`). Backend flash/status strings in `app/Http/Controllers/Settings/*` must use `__()` keys.

Quality: `npx tsc --noEmit` clean for your files; `npx vp check` on your paths; `DB_DATABASE=ev_test_1 php artisan test tests/Feature/Auth tests/Feature/Settings` green.
