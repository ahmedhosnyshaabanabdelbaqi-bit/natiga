# W1-C · Integrations provider layer   (test DB: ev_test_3)

Owned paths: `app/Modules/Integrations/**`, `database/factories/Integrations/**`, `lang/{ar,en}/integrations.php`, `resources/js/pages/admin/integrations/**`, `resources/js/features/integrations/**`, `tests/Feature/Integrations/**`, `tests/Unit/Integrations/**`, `docs/modules/integrations.md`.

State: backend is largely complete (contracts + DTOs, NotConfigured/Manual/OSM/LaravelMail/Log drivers, IntegrationManager + `Integrations` facade, `IntegrationCall`, webhook ingest + `ProcessWebhookEventJob` + `WebhookHandlers`, `ExchangeRates`, `integrations:check`, permissions) and 114 of 118 current tests pass. The remaining failures are only the missing Inertia pages. `config('ev.integrations.map')` now exists (driver `osm` from `MAP_PROVIDER`, `nominatim_url`, `nominatim_rate_per_second`) — use `config('ev.integrations.map.nominatim_url')` instead of the hardcoded constant.

To finish (verify each):
1. Admin pages (Inertia, bilingual, responsive, states): `resources/js/pages/admin/integrations/index.tsx` — status matrix (integration, category, driver, configured?, status badge + last check, last success, last error truncated, fallback text, docs link), "Run check" per row and "check all" (integrations.manage), "Send test email to me" (shows Not Configured clearly), geocode test input for the map provider, tab/link to webhook events; `admin/integrations/exchange-rates/index.tsx` — latest rate cards per currency, history table, add-manual-rate form with reason (exchange_rates.manage); `admin/integrations/webhook-events/index.tsx` — filterable list, detail drawer (sanitized payload), retry (integrations.manage). Match the props the controllers already send (read the controllers).
2. `Integrations::matrix()` used by the page and by the future readiness report; ensure the Reports/Operations bridge only calls `OperationsExceptions` when the class exists.
3. `docs/modules/integrations.md`: contracts, adding a vendor driver, env variable names per integration, webhook URL `/webhooks/{provider}`, signature verification, retry/failure behaviour, testing with `Http::fake`, exchange-rate convention (base = foreign currency, quote = EGP, 1 USD = 48.5 EGP → base=USD, quote=EGP, rate=48.5).
4. Re-run the whole Integrations suite and fix anything failing. Add tests if any brief item is untested: default drivers NotConfigured/Manual; OSM geocode with Http::fake + caching + graceful failure; haversine Cairo→Alexandria ≈ 179–182 km; webhook idempotency, bad signature → 401 + flagged row, retry failed→processed; exchange rate lookup by date + snapshot (250 USD @ 48.00 = 12,000.00 EGP); manual rate needs permission + audit; email isConfigured false for log/array, true with force flag.
