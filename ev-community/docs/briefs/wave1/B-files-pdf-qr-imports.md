# W1-B · Files, PDF, QR/barcode, Imports & Exports   (test DB: ev_test_2)

Owned paths: `app/Modules/Files/**`, `app/Modules/Imports/**`, `app/Support/Pdf/**`, `app/Support/Qr/**`, `app/Support/Barcode/**`, `config/filesystems.php`, `resources/views/pdf/{layout,sample}.blade.php`, `resources/fonts/**`, `database/migrations/2026_01_01_000700_core_create_qr_tokens_table.php`, `2026_01_24_000100_imports_create_imports_tables.php`, `database/factories/{Files,Imports}/**`, `lang/{ar,en}/{files,imports,pdf}.php`, `resources/js/pages/admin/{imports,exports}/**`, `resources/js/features/{files,imports}/**`, `tests/Feature/{Files,Imports}/**`, `tests/Unit/{Support,Files,Imports}/**` (keep `tests/Unit/Support/MoneyTest.php` intact), `docs/modules/files-and-imports.md`.

Deliverables (audit what exists, finish the rest):
A) Files: `Attachment` model (HasPublicId, owner morph); `Contracts/HasAttachments` (`attachmentViewableBy(User, Attachment): bool`) + `Concerns/HasAttachmentsTrait`; `AttachmentService::store(UploadedFile, ?Model $owner, string $collection, string $visibility='private', string $kind='document'|'image'|'spreadsheet', ?User $uploader=null)` with finfo MIME sniffing, extension allow-list from `config('ev.uploads')`, size ceilings + operator limits (`files.max_image_mb`, `files.max_document_mb`), empty-file rejection, random names `{yyyy}/{mm}/{ulid}.{ext}` on the private disk, sha256, image meta, WebP variants thumb 320/medium 800/large 1600 (intervention/image v3, GD); `delete`, `url`, `downloadUrl` (= `route('shared.files.download', $a)`), `temporaryUrl`, `moveToOwner`; rule `Rules/SafeUpload`; `FileDownloadController` GET `/files/{attachment:public_id}` (auth; uploader, or owner's `attachmentViewableBy`, or `files.manage`; else 403; inline for images/PDF; `?variant=`); `UploadController` POST `/files/upload` (throttle:uploads, collection pending_upload); `files:purge-orphans {--force}` (dry-run default) scheduled daily; permission `files.manage`.
B) `PdfService::render(view, data, locale)` / `store(...)` via mpdf 8 with Cairo TTFs from `resources/fonts/cairo` (present), RTL for ar; `resources/views/pdf/layout.blade.php` bilingual with header (site name from Settings), footer page numbers + generated-at; `pdf:test {--locale=ar}`.
C) `QrService` (opaque signed tokens backed by `qr_tokens`: `token(purpose, payload, ttlSeconds, singleUse)`, `verify(token, purpose)` atomic single-use, `svg`, `pngDataUri`, `qr:purge-expired` daily) and `BarcodeService` (`code128Svg`, `code128Png`, `qrLabelSvg`).
D) Imports/Exports: `Contracts/Importer` & `Exporter`, registries `Importers`/`Exporters`, `ImportService` (upload CSV/XLSX via openspout, BOM + Windows-1256 fallback, header mapping by key or AR/EN label → preview → validate (queued > 500 rows) → confirm → `ProcessImportJob` chunked, idempotent per row, error-report CSV attachment, invariant imported + rejected + skipped + duplicate == total_rows asserted) → cancel; reference importer(s) (currencies/exchange rates); `ExportService::request(type, filters, format, user)` (permission per exporter, inline ≤ 2000 rows else `ProcessExportJob`, private attachment, expires 24h, `exports:purge-expired`, audit `exports.generated`, locale headers); admin pages `/admin/imports` (index, wizard: type → upload → mapping preview → validation summary + per-row errors + error report download → confirm → progress polling → final report, show) and `/admin/exports` (list + download) + `POST /admin/exports`; permissions `imports.manage`, `exports.view`.
E) `docs/modules/files-and-imports.md`.

Tests: valid jpeg/png/pdf/csv uploads; reject .exe, PHP-disguised .jpg, oversize, empty; image variants; private download IDOR (403 other user, 200 uploader/owner-authorised); purge command; PDF starts with %PDF and > 5 KB Arabic, both locales; QR valid/tampered/expired/single-use twice/wrong purpose; CSV + XLSX import parse, AR/EN header mapping, invalid rows reported, duplicates, invariant, unauthorized 403; export permission + filters, large export queued, expired export not downloadable.
