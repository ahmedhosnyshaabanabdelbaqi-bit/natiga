# W1-G · Notifications: in-app center, channels, preferences, announcements, email templates   (test DB: ev_test_7)

Owned paths: `app/Modules/Notifications/**`, `database/migrations/2026_01_22_000[1-4]*_notifications_*.php`, `database/factories/Notifications/**`, `database/seeders/Notifications/**`, `lang/{ar,en}/notifications.php`, `resources/views/mail/**`, `resources/js/pages/{admin,member,partner}/notifications/**`, `resources/js/features/notifications/**`, `tests/Feature/Notifications/**`, `tests/Unit/Notifications/**`, `docs/modules/notifications.md`.

State: backend partially written (~37 PHP files, three migrations, mail views, lang files with key parity). A PHP parse error in `AnnouncementService` was fixed. No pages, no tests. The Integrations module now exists: use `App\Modules\Integrations\Services\Integrations::email()->isConfigured()` (and `sms()`, `whatsapp()`), honour `config('ev.integrations.email.force_configured')`. Audit and complete:
1. `Notify::send(User $user, string $key, array $data = [], string $category = 'system', bool $transactional = true, ?string $dedupKey = null, ?string $url = null, ?array $channels = null)` — title/body per user locale from `notifications.templates.<key>.title|body` or `$data['_title_key']/['_body_key']`; stores the in-app notification (unique (user_id, dedup_key) → duplicate returns the existing row with no new deliveries); delivery rows per channel: in_app 'sent'; email only if configured AND (transactional OR preference enabled); sms/whatsapp only if the provider is configured AND marketing consent (consent_logs) AND preference, else 'skipped' with reason; queued jobs SendEmail/SendSms/SendWhatsApp (re-check status before sending, 3 tries, backoff 30/120/600, final failure → failed + error). `sendMany` chunked; `markRead`, `markAllRead`, `unreadCount` (cached 60 s, invalidated on write). `NotificationMail` + `resources/views/mail/layout.blade.php` (bilingual, RTL for ar, branding, text alternative). `EmailTemplateRenderer`: admin templates override lang defaults; `{{variable}}` placeholders escaped; unknown → blank.
2. Preferences `/account/notification-preferences` (category × channel; transactional locked on; marketing opt-in; audited `notifications.preferences_updated`); `NotificationPreferences::allows(user, category, channel)`.
3. Notification centers `/account/notifications`, `/admin/notifications/inbox`, `/partner/notifications` (one shared component in `resources/js/features/notifications`, thin pages): newest first, category filter, mark read on click, mark all read, pagination, unread badge; shared prop `unreadNotifications` via `Inertia::share` (cheap cached count); JSON `…/unread-count` endpoints for 60 s polling.
4. Announcements `/admin/notifications` (notifications.manage; notifications.view read-only): list; create/edit (bilingual title/body, url, category, marketing flag, channels — email/sms/whatsapp selectable only when configured, else "Not configured" badge; audience: all_members | vehicle_make | vehicle_model | specific_members (member numbers resolved server-side) | pluggable via `AnnouncementAudiences::register(key, labelKey, fn(array $params): Builder|iterable, ?module)`); server-computed estimated recipients; AR/EN preview; send now or schedule; `SendAnnouncementJob` (500-recipient chunks, `announcement_recipients` unique, `Notify::sendMany` with dedup `announcement:{id}` so re-runs never duplicate, counters); cancel scheduled; `notifications:dispatch-scheduled` every minute; audit entries.
5. Email templates `/admin/notifications/templates`: list (defaults from lang + `TemplateRegistry` keys/variables declared by modules), edit subject/body per locale with variable chips, live preview with sample data, reset to default, reject HTML/script in bodies (plain text + line breaks + limited bold/links rendered safely).
6. `notifications:purge` weekly (read notifications older than `notifications.retention_days`, default 180); Settings keys `notifications.retention_days`, `notifications.email_from_name_ar/en`; KPI `failed_deliveries_24h` (notifications.view); permissions notifications.view (operations-manager, support-agent), notifications.manage (operations-manager, content-manager).
7. `docs/modules/notifications.md` (how modules send notifications: keys, categories, dedup keys, urls; registering audiences and template keys; channel rules; retry semantics). In the final report, note that the integration pass must feed `unreadNotifications` into the portal layouts.

Tests (Queue::fake / Mail::fake): dedup (one row, one delivery set); transactional email queued when configured (force flag) and skipped when not; marketing email skipped without consent, sent with consent + preference; sms skipped not_configured; unread count; member A cannot mark B's notification (403/404); announcements: all_members = active members only, specific members by number, estimated count, job re-run idempotent, permission enforced; template rendering escapes `<script>`; preferences cannot disable transactional in_app/email (422).
