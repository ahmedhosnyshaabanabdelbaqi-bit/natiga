# W1-F · Vehicles master data, member vehicles, My Garage, public vehicle selector   (test DB: ev_test_6)

Owned paths: `app/Modules/Vehicles/**`, `app/Modules/Garage/**`, `database/migrations/2026_01_03_*_vehicles_*.php`, `database/factories/Vehicles/**`, `database/seeders/Vehicles/**`, `lang/{ar,en}/{vehicles,garage}.php`, `resources/js/pages/admin/{vehicles,garage}/**`, `resources/js/pages/member/garage/**`, `resources/js/pages/public/vehicles/**`, `resources/js/features/{vehicles,garage}/**`, `tests/Feature/{Vehicles,Garage}/**`, `tests/Unit/Vehicles/**`, `docs/modules/vehicles-garage.md`.

State: backend largely written (~50 PHP files in Vehicles, ~17 in Garage, both migrations; services, presenter, garage stats, providers). No pages, no tests; check whether `database/seeders/Vehicles/VehiclesMasterSeeder.php` exists (the `MasterDataSeeder` auto-calls `database/seeders/*/*MasterSeeder.php`). Audit and complete per `docs/DOMAIN_CONTRACTS.md` (Vehicles + Garage rows are binding for other modules):
1. Models & rules: `MemberVehicle` (`scopeForUser`, `displayName()`, `connectorTypeIds()`, `compatibleStationConnectorTypeIds(): {direct, adapter}`, `isOwnedBy`), VIN encrypted (`encrypted` cast) + `vin_hash` (sha256 of normalized VIN) for duplicate detection, VIN masked (last 4) with audited reveal (`vehicles.vin_revealed`), odometer history (decrease requires a reason), `User::resolveRelationUsing('vehicles', …)`, policies (owner only; staff with vehicles.view read-only).
2. Master data seeder (idempotent `updateOrCreate`, master data only): connector types type2, ccs2, chademo, gbt_ac, gbt_dc, nacs; compatibility rules (same type direct; type2↔gbt_ac adapter; ccs2↔gbt_dc incompatible; chademo↔ccs2 incompatible; nacs↔ccs2 adapter); makes/models/variants common in Egypt (BYD, MG, Tesla, Nissan, Hyundai, Kia, Volkswagen, Chery/Omoda, Geely, Zeekr, Xpeng, Smart, Volvo, BMW, Mercedes, Peugeot, Citroën, Renault, Leapmotor, GAC Aion, Neta, Dongfeng) with market versions (china GB/T vs europe/gulf Type2+CCS2), year ranges, approximate battery capacity and a note "approximate public spec; verify".
3. Admin: `/admin/vehicles` (makes CRUD with logo, models per make, variants per model with connectors/battery, connector types + compatibility matrix editor setting verified_by/at, member vehicles list `/admin/vehicles/members` with search by member number/name and VIN lookup by hash — VINs never listed; read-only detail with odometer history; all audited `vehicles.master_changed`), `/admin/garage` (counts by make/model top 20, vehicles without variant, by year). Permissions vehicles.view, vehicles.manage_master, vehicles.edit_member_vehicle. KPI `vehicles_total`.
4. Member: `/account/garage` (cards grid, empty state CTA), add-vehicle wizard `/account/garage/create` (make → model → variant or "I don't know" → year → market version → battery → optional VIN (17 chars, no I/O/Q; exact duplicate across active vehicles blocked with a clear message) → nickname/color → image ≤ 10 MB via `AttachmentService`), edit, archive/sold, set primary, delete guarded by `VehicleDeletionGuards::register(fn(MemberVehicle): ?string)`; vehicle page `/account/garage/{vehicle}`: header (image, name, status, odometer + update dialog) and a tab list driven by `Garage\Services\GarageSections::register(key, labelKey, resolver(MemberVehicle, User): ?array, module, order)` resolved lazily (Inertia deferred/optional props); React renders `resources/js/features/garage/sections/<key>.tsx` per key with a generic empty-state fallback for keys without a component. Build sections `info`, `odometer`, `charging_compatibility`.
5. Public: `GET /{locale}/vehicles/data` (cached 1 h, localized makes/models/variants, name `public.vehicles.data`), `/{locale}/vehicles` (supported vehicles page), `POST /{locale}/vehicles/select` (session selection for guests), shared prop `selectedVehicle` (member's primary vehicle, else session selection), hook `useSelectedVehicle()` and component `VehicleSelector` in `resources/js/features/vehicles/`.
6. `docs/modules/vehicles-garage.md` documenting GarageSections, VehicleDeletionGuards, selectedVehicle for other engineers.

Tests: IDOR on vehicles by public_id (view/update/delete 403); VIN encrypted at rest (raw column ≠ VIN) and hash matches; duplicate VIN blocked; odometer decrease needs reason and writes history; master CRUD needs vehicles.manage_master; seeder idempotent (twice → same counts); CCS2 vehicle vs gbt_dc incompatible, type2 vs gbt_ac adapter; garage sections only for enabled modules; public data endpoint cached and localized.
