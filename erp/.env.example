# Copy to .env before running docker compose.

# Required — the stack refuses to start without it, so no deployment ships a
# password that is written down in a repository.
ERP_OWNER_PASSWORD=
ERP_OWNER_EMAIL=owner@example.test

DB_PASSWORD=erp
DB_PORT=5432
API_PORT=8000
WEB_PORT=5173
