import { Module } from '@nestjs/common';

/**
 * RSS/Atom import through SafeFetchService; items become drafts only (never auto-published).
 *
 * Skeleton created by the backend foundation and registered in AppModule.
 * NOT IMPLEMENTED YET: controllers/providers are added by the owning team
 * inside src/modules/rss-import/ only.
 */
@Module({})
export class RssImportModule {}
